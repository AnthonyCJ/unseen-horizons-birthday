# Public project guardrails

- Treat the imported V38 experience as the product baseline; V39 is an engineering migration.
- Preserve the five-screen order, page 02 one-way ritual, the 15.6/20.6-second timing chain, BGM reset semantics, paper transitions, reading timing, and the final smile animation.
- Keep the public base path at `/unseen-horizons-birthday`.
- Never commit a plaintext access key, private handoff material, archive, candidate asset, temporary image, Sites configuration, credential, or token.
- Keep only the three referenced production images and the production web audio file under `public/assets/`.
- The fragment gate is a privacy deterrent, not authentication; do not describe it as access control.
- Keep metadata and the invalid entrance neutral, with `noindex, nofollow`.
